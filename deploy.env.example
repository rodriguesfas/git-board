# =============================================================================
# Configurações de Deploy - Git Board
# Copie este arquivo para deploy.env e configure suas credenciais
# =============================================================================

# Configurações SSH
SSH_HOST=seu-servidor.com
SSH_USER=seu-usuario
SSH_PASSWORD=sua-senha
SSH_PORT=22

# Diretório de destino no servidor
REMOTE_DIR=/public_html/git-board

# =============================================================================
# INSTRUÇÕES DE USO:
# =============================================================================
# 1. Copie este arquivo: cp deploy.env.example deploy.env
# 2. Edite o arquivo deploy.env com suas credenciais
# 3. Execute: source deploy.env && ./deploy.sh
# 
# OU configure as variáveis de ambiente diretamente:
# export SSH_HOST=seu-servidor.com
# export SSH_USER=seu-usuario
# export SSH_PASSWORD=sua-senha
# ./deploy.sh
# =============================================================================
